p
  | #{answer.body}
  - if answer.user == current_user
    = link_to 'Edit', '#', class: 'answer-link edit-answer', data: { answer_id: answer.id }
    = link_to 'Delete Answer', answer_path(answer), method: :delete, class: 'answer-link'
    = render 'answers/form', answer: answer
